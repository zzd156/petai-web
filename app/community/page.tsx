import { PageContainer } from "@/components/PageContainer";

export default function CommunityPage() {
  return (
    <PageContainer
      title="社区"
      description="社区页面骨架：后续可接入帖子流、话题标签、互动评论等模块。"
    />
  );
}
